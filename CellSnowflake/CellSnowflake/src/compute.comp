#version 430 core
//ïœêîégÇ¶Ç»Ç¢
 const uint localSizeX = 100;
 const uint localSizeY = 1;
 const uint localSizeZ = 50;

layout(local_size_x = 100, local_size_y = 1, local_size_z = 50) in;

class Cell
{
	vec4 position;
};

layout(std430, binding = 0) buffer Cells
{
  Cell cell[];
};

void main()
{
	//ÉGÉâÅ[
	//const uint testNum = gl_WorkGroupID.x;
	const uint i = 5000;

	cell[i].position.z += 0.1f;
  
}
