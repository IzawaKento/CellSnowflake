#version 430 core

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

struct Cell
{
	vec4 position;
	/*
	uint flags;
	float diffusionMass;
	float boundaryMass;
	int horizontalNeighbourNum;
	int verticalNeighbourNum;
	*/
};

layout(std430, binding = 0) buffer Cells
{
	Cell cell[];
};

void main()
{
	const uint i = gl_WorkGroupID.x;
	cell[i].position.y = 9.0;
}
